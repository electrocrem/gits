[Unit]
Description=hypridle (dim, lock, screen off, sleep timers: gits-idle)
PartOf=gits-session.target

[Service]
ExecStart=/usr/bin/hypridle
Restart=on-failure
RestartSec=1

[Install]
WantedBy=gits-session.target
