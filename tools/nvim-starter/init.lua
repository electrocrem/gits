-- Ghost in the Shell: there was no Neovim config, so the installer put the LazyVim starter here (the GitS colours, dashboard,
-- statusline and bufferline in lua/plugins/gits.lua are LazyVim plugin specs). Your own init.lua would have been left alone.
require("config.lazy")
